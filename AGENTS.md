# 项目开发原则

## 项目背景

本项目面向平安寿险一线代理人，目标是在口袋E App 中提供智慧经营智能体 Demo。智能体不是一次性的客户推荐器，也不是只生成营销话术的聊天助手，而是围绕代理人的阶段经营目标，持续协助完成：

```text
明确经营目标 → 发现经营机会 → 找到具体客户 → 制定个性化经营策略
→ 校验是否可经营 → 形成经营任务 → 追踪反馈 → 调整剩余目标并再次经营
```

系统以代理人的自然语言 Goal 为入口，由 Agent 在运行时组合可复用的 Capability 和 Tool。Goal 可以包含经营阶段、目标指标、期限、主推方向、渠道和其他约束；这些信息必须被解析为可追踪、可解释的结构化经营目标，而不能只停留在 Prompt 或页面文案中。

Demo 以“开门红”作为演示场景，示例输入可以是“目标500W NBEV，主推御享分红26和御享金越年金”。这只是用于展示通用能力的样例，不是系统内置的唯一场景、目标或产品。产品适配、机会判断、客户事实和业务规则必须来自版本化数据、知识、模型或确定性规则服务，不能写死在 Agent、前端或 Prompt 中。

当前 Demo 的渠道边界为个险，经营人员默认为当前登录代理人；银保客户等不属于当前代理人可经营范围的对象，必须在创建经营任务或分发前由确定性规则阻断。系统应清楚区分客户事实、规则结果、模型信号和 Agent 建议，并保留来源、版本、时间和可审计的运行轨迹。

## 核心开发规则与约束

@doc/智慧经营智能体开发规则与约束.md

## 智能体实现方向

智能体相关代码参考 Pi-Agent 所体现的分层思想，但不照搬其具体目录、类型或实现。

关注点可以从概念上分为：

1. 模型调用：负责 LLM 接口通信。
2. Agent 运行机制：负责循环、消息、状态、工具和事件。
3. 业务智能体：负责组合具体业务能力、提示词和约束。

分层的核心要求是依赖方向清晰：底层能力不应感知具体业务，上层可以组合和扩展底层能力。层数、目录和类型设计应根据实际复杂度决定，而不是预先固定。

优先自行实现透明、可控的 Agent Loop 和工具调用机制。未经用户确认，不引入 LangGraph、LangChain Agent 等重量级 Agent 编排框架。

除主 Agent 外，业务能力和外部能力统一按可插拔 Tool 设计，并通过稳定的工具协议完成注册、发现、参数校验、调用和结果返回。Tool 的内部实现可以是本地确定性函数、外部服务适配器或独立的大模型调用，但不得要求主 Agent 感知其具体实现方式。工具协议优先与 OpenAI Tools 调用形态保持兼容。

插件化首先表示实现可替换、依赖方向清晰和调用过程可观察，不等于预先建设动态安装、复杂生命周期管理或重量级插件框架；只有真实需求出现时才扩展这些能力。

不追求完全自治。评分关键路径可以由代码保证必要步骤，模型在适合的范围内自主选择工具和组织结果。

## 工程约束

- 当前运行环境为 Windows 11，只使用 PowerShell 命令和 PowerShell 脚本。
- 所有密钥、令牌和服务地址通过环境变量或本地配置提供，不写入仓库。
- 不主动引入微服务、消息队列、多 Agent、复杂工作流引擎等基础设施，除非真实需求证明其必要性。
- 新增抽象或依赖时，应能够说明它解决的当前问题。
- 优先为确定性业务逻辑和 Agent 核心机制编写测试。
- 修改实现时同步维护必要的测试和文档。

## 文档职责与事实源

文档按职责分开维护：

- `doc/current-state.md`：面向新会话的当前实现快照。
- `doc/architecture.md`：当前真实的目录、组件、依赖边界和主要数据流。
- `doc/智慧经营智能体 Demo 故事.md`：当前 Demo 面向用户展示的业务故事、经营闭环和演示场景。它用于说明产品体验与验收叙事，不得单独替代代码、事实数据或确定性业务规则。
- `doc/智慧经营智能体开发规则与约束.md`：智慧经营智能体的产品、算法、后端、前端、测试和架构开发基线。它定义 Goal、Plan、Opportunity、Capability、Tool、规则校验、数据来源、审计、通用性和 Demo/生产边界等约束；与本文件冲突时，以本文件中的项目级操作约束和更高优先级指令为准，并应记录冲突。
- `doc/design/`：尚在探索或等待确认的设计方案。
- `dev-log.md`：工程变更、里程碑、验证结论和相关文档索引。

上述文件或目录尚不存在时，按需创建；不得因为文档缺失而虚构当前实现事实。早期讨论稿不是当前实现事实源或强制规范；后续继续讨论候选方案时，应将内容写入或迁移到符合本规则的位置。

新会话开始开发前，应优先阅读仓库中已经存在的相关文档，顺序为：

1. `doc/current-state.md`
2. `doc/architecture.md`
3. `doc/智慧经营智能体开发规则与约束.md`
4. `doc/智慧经营智能体 Demo 故事.md`
5. 当前任务相关的 `doc/design/*`
6. `dev-log.md` 中的相关历史记录

文档描述冲突时，按以下顺序校准：

1. 已验证的当前代码和运行结果
2. `doc/current-state.md`
3. `doc/architecture.md`
4. `dev-log.md`

未确认的 `doc/design/*` 不覆盖当前事实或已确认决策。如果较早文档与当前实现不一致，不得直接照旧文档开发；应先核实代码和验证结果，再校准当前状态与相关决策记录。

## 设计文档规则

- 面向后续探索的新设计文档统一放在 `doc/design/`。
- 文件名以创建日期开头，格式为 `YYYY-MM-DD-<topic>.md`，其中 `topic` 使用简短、可检索的 kebab-case 英文名称。
- 同一主题继续讨论时优先更新已有文档，避免按会话重复创建；只有问题域或方案边界发生实质变化时才新建文档。
- 文档开头必须标明状态，例如“探索中”“待确认”“已确认”或“已废弃”。
- 未确认设计仅用于讨论，不能覆盖当前事实源和已确认 Decision，也不能被当作执行指令。
- 设计文档重点说明问题、目标与非目标、系统边界、职责划分、数据或控制流、不变量、取舍、风险、开放问题和成功标准。
- 除非用户明确要求且方案已经确认，不把设计文档写成实现规格；避免提前写入代码片段、类型或函数签名、类名、逐文件修改清单、具体库 API、施工步骤和未经验证的固定参数。
- 可以引用当前已经存在的文件名、接口名和数据结构解释现状，但不得借此预先锁定未来实现。

## 当前状态文档规则

- `doc/current-state.md` 只记录现在成立且已经验证的事实，不记录实现过程、阶段复盘或新旧状态并列描述。
- 实现发生变化时，直接替换与现状冲突的旧描述，避免追加“后来改为”等时间线叙述。
- 内容保持精炼，重点覆盖系统快照、当前能力、核心契约、系统边界、已知限制、任务入口和最近验证基线。
- 不写逐组件实现说明、完整文件清单、本机路径、完整命令过程或常规调试流水。
- 当前目录和依赖关系写入 `doc/architecture.md`；历史过程和验证明细写入 `dev-log.md`。
- 最近验证基线必须注明时间或时间范围；本轮未重新执行的检查不得描述为“本轮通过”。
- 只有当前能力、接口或状态契约、系统边界、生产限制、任务入口或验证基线发生变化时才更新；纯格式调整和内部等价重构无需扩写。
- 更新后应搜索并清除与新实现矛盾的旧术语、旧接口名和旧数据流描述。

## 开发结束后的文档同步

任何代码修改完成后，必须检查并按实际影响更新：

1. `doc/current-state.md`：当前能力、核心契约、系统边界、限制、任务入口或验证基线是否变化。
2. `doc/architecture.md`：目录、组件职责、依赖方向或主要数据流是否变化。
3. `dev-log.md`：本次功能或行为变更、必要的决策引用和验证结论是否需要记录在 `[Unreleased]`。

仅修改文档格式、措辞或探索性内容时，不得虚构代码变更、验证结果或开发日志记录。

## 决策方式

当需求尚未明确时：

- 先区分已确认事实、当前假设和待确认事项。
- 优先选择容易替换和撤销的方案。
- 在做出会显著限制后续设计空间的决定前，与用户确认。
