# 留空时使用同源 /api 端点（Vite 开发服务器会代理到 127.0.0.1:8000）。
# VITE_API_BASE_URL=http://127.0.0.1:8000

# 后端流式路径，默认 /api/chat/stream。
# VITE_CHAT_STREAM_PATH=/api/chat/stream

# 当前演示数据源；替换数据集时无需修改页面代码。
# VITE_DATA_SOURCE_ID=demo-acts-1-3
